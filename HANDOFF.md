# 工作交接 · 1.0.4 阅读模式

更新：2026-09-24。本轮基线提交 `f3bd6d7`。后续提交号以 `git log -1` 为准。

## 先读这里

唯一源码是当前 `Jijian-Translate/` 仓库。外层 `archive/` 仅供追溯；`dist/` 由外层 `scripts/build-local.mjs` 生成。不要修改生成目录，也不要从历史文件夹恢复旧实现。

本轮需求：修复大纲颜色按钮重叠；扩大百度百科结构调查；把后续需求拆清楚，方便换模型。**没有要求一次性完成所有百科适配。**

- [调查清单与 16 个可点击样本](docs/handoff/BAIKE-SURVEY.md)
- [后续任务、代码入口和验收条件](docs/handoff/BAIKE-TASKS.md)
- [检查方法与已完成验证](docs/handoff/VALIDATION.md)

## 当前进度

已完成：维基百科延伸阅读与补充表格、组合棋盘、译文上标引用、表格原有彩色单元格、整行大纲点击、圆形纸张色块；百度百科摘要、基本信息、作品与图集的首轮提取。以上不等于全部网页适配完成。

本轮已完成：删除大纲颜色按钮组残留的旧下拉框 CSS。旧 ID 规则的高优先级 `display:block` 覆盖新网格，造成按钮拥挤、重叠；现在统一两列网格及边界尺寸。12 种窗口／侧栏宽度／明暗主题组合通过布局断言，键盘选择也通过。

本轮调查：16 个百度百科词条均能进入阅读模式，已采集章节未发现缺失，但层级、滚动定位、全部正文与媒体完整性仍需要专项检查。观察到 14 种 `data-module-type`。记录原网页结构，不复制全文，不截图。

尚未完成：基础信息逐字段语义核对、目录层级与滚动位置核对、结构化时间线样本确认、作品类型化排版、图集懒加载与重复提取处理、正文内视频适配。见任务表，不要把调查结果当成修复完成。

## 更新计划

1. 优先 B01：基本信息；再做 B02：目录与跳转。每项独立复现、修改、验证、记录。
2. B03–B05：表格／图片归属、作品卡、图集。保留已满意的阅读布局，避免全局重写。
3. B06：先确认时间线／大事记的真实结构，再决定如何保留。不能把“历史沿革”标题或互动图鉴当成时间轴。
4. B07：正文视频与地图等降级展示；最后回归维基百科与普通文章，交维护者抽查。
5. 当前保持 1.0.4 开发候选。完成关键项后再打候选包，不自动发布商店或创建正式 Release。

GitHub 最新公开 Release 最后确认为 1.0.2。商店最近确认公开版本为 1.0.1，维护者提交过 1.0.2；1.0.3 曾收到说明关键词拒审通知。后续审核结果未知，以后台为准。Git 提交、候选 ZIP 和商店发布是不同状态。

## 给接手模型的任务提示

> 阅读 AGENTS.md、HANDOFF.md、docs/handoff/BAIKE-TASKS.md。先只完成 B01。先检查当前代码和对应真实页面，记录可复现差异，构造最小自有文本 fixture，再做局部修改。不要重写整个阅读器，不要修改 dist，不要扩大到其他任务。运行发布与翻译核心审计及对应原生扩展 DOM 回归，不做截图检查。结束时更新任务状态，写出实际修复、证据、未解决项；未验证的内容明确标注。若样本结构已变化，更新证据后再改，不照搬哈希类名。

## 产品约束

简体中文沟通，中英文间留空格。阅读界面小字至少 13 px；设置与 Popup 保留系统字体，不重新全局套用得意黑。正文按语种回退。保留简洁、黑白为主的控件设计。互动图鉴和相关星图可跳过；有意义的时间、作品、表格和正文不可因链接多而整块删除。无需截图验收，维护者做视觉抽查。
