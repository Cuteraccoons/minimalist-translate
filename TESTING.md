# 安装后的快速验收页

建议每次更新扩展后，用下面 5 个页面做一轮 10 分钟冒烟测试。

1. `https://en.wikipedia.org/wiki/Anthropology`
   - 测试长文双语翻译、替换翻译、阅读模式、大纲长标题和正文图片。
2. `https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Introduction`
   - 测试多层标题、代码块、站内导航与双语 / 替换 Tab 切换。
3. `https://docs.python.org/3/tutorial/index.html`
   - 测试长目录、代码片段、链接和文档型网站的原位翻译。
4. `https://web.dev/articles/responsive-images`
   - 测试响应式图片、长页面滚动时图片翻译入口是否始终留在图片内。
5. `https://xkcd.com/927/` 与 `https://xkcd.com/1425/`
   - 测试英文漫画 OCR、同行文字合并、译图排版、原图 / 译图切换和下载。

## 最小检查顺序

- 在图片内移动鼠标：入口只出现在该图右下角；离开图片后立即消失。
- 打开阅读模式，再点图片进入放大查看：点击「翻译」，扫描层应直接出现在放大图上方。
- 首次下载 OCR 模型：说明与进度始终共用左上角同一个窗口；点击下载后窗口只剩进度条。
- 译图完成后滚动页面：图片仍留在文章原位置；切换“原图 / 译图”不改变布局。
- 完成后：左上角 OCR 状态消失；移入图片才显示右下角操作区。
- 在 Popup 切换“双语对照 / 替换原文”：指示器应等宽、平滑地移动到目标 Tab，不闪烁、不叠出多层底色。
- 在 Popup 点击“本网站图片翻译”：状态应在“已开启 / 已关闭”间切换，并立即隐藏或恢复当前域名的图片入口。
- 在 Popup 展开下划线颜色、中文字体和译文颜色：选单四边均完整显示，不被卡片裁切。
- Popup 搜索英文单词：句号、分号分隔的义项应分行显示。
- 在页面靠近屏幕底部的位置划词：查词窗应自动上移；拖动标题栏时整窗跟随，正文很长时仍可在窗内滚动。
- 在设置页依次展开查词交互、查词引擎和 OCR 语言等选单：选项应浮在卡片上方且完整可点。
- 进入“排版与呈现”和“生词本”：切换按钮默认有选中态，字号、间距滑杆和卡片 / 列表视图均可操作。


## 1.0.3 候选验证（2026-09-17）

按维护者要求，本轮没有截图验收。安装候选目录后，从「设置 → 项目与赞赏 → 阅读测试台」逐个打开实站，记录问题并导出 Markdown。

- 发布审计、翻译核心审计、笔记隔离检查、收藏筛选检查、OCR 模型下载／缓存单测均通过。
- 真正加载扩展的 Chromium：英文 OCR 识别出 `HELLO / SECOND CARD`；简体中文识别出“你好世界 本地文字识别”。模型首次下载和缓存复用均成功；屏蔽后台网络后，英文仍可使用缓存识别。不是模拟 OCR。
- 真实 Gutenberg《原始文化》卷一：提取 1,317 个段落块、约 110 万字符，页码与站点起止说明已移除。
- 真实青空文库《我是猫》：提取 2,256 个段落块、11 个章节，保留 9,214 个 ruby 注音。上述数量是当前页面结果，不保证远端以后不变。
- 长书本地回归使用模拟翻译：316 个段落首次只请求视口附近 12 段，重复正文保留，脚注可跳转。没有请求整本书的真实翻译服务。
- 欢迎指南在原生扩展中验证网页翻译、分栏、阅读模式与笔记读取；示例译文为内置文本，不能作为在线翻译服务可用性证据。
- 收藏页网站筛选、CSV／Markdown 下载和测试台备注持久化通过浏览器交互检查。

新增脚本：`tests/collection-filters-audit.mjs`、`tests/ocr-models-audit.mjs`、`tests/playwright-reader-books.js`、`tests/playwright-ocr-native.js`、`tests/playwright-welcome.js`、`tests/playwright-collections-launcher.js`。

浏览器脚本通过 Playwright CLI 的 `run-code --filename` 运行。书籍回归需在源码目录启动 127.0.0.1:8768 HTTP 服务；原生扩展脚本需独立的持久化 Chromium 测试配置，加载源码目录。收藏脚本会写入测试收藏，只能在独立测试配置中运行，不使用个人浏览器配置。`ocr-models-audit.mjs` 模拟网络和缓存；原生 OCR 脚本执行真实本机识别。
