# Chrome Web Store 发布清单

## 版本状态

- 扩展名称：极简翻译 | 双语网页翻译与沉浸阅读
- 当前版本：1.0.0
- 文档更新：2026-08-27
- 发布状态：草稿，暂不提交审核
- 主要语言：简体中文
- 分类：生产力工具
- 发布者：龙猫君
- 联系邮箱：待开发者账号开通后填写
- 主页地址：`https://github.com/Cuteraccoons/minimalist-translate`（仓库公开后启用）
- 隐私政策地址：待仓库公开或独立网页上线后填写

## 商店文案

### 简短说明

面向网页阅读的开源翻译扩展。提供原网页双语、沉浸阅读、本地 MDX 词典与本机图片 OCR，无需注册，数据本地优先。

### 单一用途

在网页原有阅读流程中提供翻译、对照阅读与上下文查词。

### 完整说明

极简翻译是一款面向外文网页阅读的开源 Chrome 扩展。它尽量保留原网页的链接、按钮、Tab 与排版关系，并在同一页面中提供双语对照、替换原文、沉浸阅读和分栏对照。

主要功能：

- 原网页双语、替换原文、沉浸阅读与分栏对照四种阅读方式。
- 空间充足的纵向导航保留双语；复杂 Tab、固定导航和尺寸受限控件自动使用单行译文，悬停或键盘聚焦可临时查看原文。
- 文章列表与推荐卡片保留原文和链接，并把译文放在同一链接内，不再误套用导航栏的原地替换规则。
- 对深浅色卡片、渐变背景、浮动图片、图注与动态加载内容自动适配，减少文字遮挡、错位和不可读情况。
- 沉浸阅读保留正文链接与配图，GIF 动图维持动态显示，图片大小随阅读区自动调整。
- 沉浸阅读自动排除 Wikipedia 等站点的编辑维护模板与机器翻译提示，不把站务说明混入文章开头。
- 沉浸阅读提供纸张、铺开、专栏与书页四种页面结构；大纲按三级标题生成并适配换行，图片阴影可在阅读器内即时开关。
- 阅读偏好侧栏不会遮暗或锁住正文，打开侧栏时仍可继续滚动阅读；Popup 与阅读器内的双语／纯中文视图控制会即时同步。
- 直接在沉浸阅读中开启双语时，译文会按批次逐步出现；纯净排版只保留链接等语义结构，不把网页局部高亮放大为整段底色。
- 图片翻译采用词级位置分割和边界内排版，避免跨栏合并与译文覆盖相邻内容；Popup 可按当前网站单独开关图片翻译入口。
- 在线多源查词、本地 MDX / MDD 词典、生词本与高亮收藏。
- 中文查词显示标准汉语拼音，并优先选择与简体中文相匹配的普通话系统音色。
- 本地词典入口和当前词典切换状态保持清晰对齐，可直接进入完整词典内容。
- 本地词典移除后可以重新选择原文件或文件夹；未配置或读取授权失效时，查词窗口会直接进入对应设置页恢复连接。
- 网页黑名单可以按网站展开独立功能设置；展开区固定显示在域名下一行，并使用明确的文字移除操作。
- AI 查词在扩展后台读取当前设备上的真实 API 配置，支持独立的自定义查词指令、完整语境解析与连续追问；回答按深绿色反白气泡分段显示（需用户自行配置）。
- 使用扩展随附的 Tesseract.js 在浏览器本机完成图片 OCR；首次按语言下载数据模型，显示阶段与数字进度，并兼容常见 CDN 图片与 SVG 图片的读取回退。
- OCR 译文按识别行写回原位置，自动处理居中、右对齐、相邻行边界和深浅复杂背景；运行错误只显示可读提示，不暴露内部代码片段。
- Google 翻译可直接使用；DeepL、DeepSeek、OpenAI、Claude、Gemini、Ollama 与自定义兼容 API 需要用户自行配置，Popup 会清晰显示当前引擎的连接状态。
- 无需注册扩展账号。API Key 与私有服务配置仅保存在当前设备。

## 权限说明

### `storage`

用于保存界面与阅读偏好、生词、高亮、翻译缓存、本地词典连接信息和用户配置。API Key、自定义服务地址、模型配置与自定义翻译指令只写入当前设备的本地存储，不通过 Chrome Sync 同步；普通界面与阅读偏好可以同步。

### `activeTab`

用于在用户主动操作当前标签页时执行翻译、阅读模式、查词和图片翻译。

### `scripting`

用于在当前网页中运行扩展的阅读与翻译界面，并保持网页原有链接和控件可用。

### `contextMenus`

用于提供右键翻译入口。

### `<all_urls>`

翻译与阅读功能由用户在任意外文网页上主动使用，因此需要覆盖不同站点；图片 OCR 还需要读取用户主动选择的跨域图片，在线翻译和查词需要访问对应服务。扩展不会在后台浏览网页，也不出售或用于广告分析网页内容。

## 数据使用与隐私

- 账号：扩展不建立账号体系，也不收集登录凭据。
- API Key：仅保存在当前设备受信任的扩展页面可访问的本地存储中，不发送给项目开发者。
- 网页文本：仅在用户执行在线翻译、在线查词或 AI 查词时发送给用户选择的服务商。
- 图片：OCR 在浏览器本机运行，原图不发送给 OCR 服务；识别出的纯文本仅在继续翻译时发送给当前翻译服务。
- 生词、高亮、缓存与本地词典：保存在当前设备，不发送给项目开发者。
- 分析与广告：没有分析 SDK、广告 SDK 或数据出售。
- 远程代码：不执行远程 JavaScript 或 WebAssembly；Tesseract 运行时代码随扩展打包。首次使用某种 OCR 语言时只下载数据模型。

完整公开说明见 `PRIVACY.md`。

## 商店素材

- [x] 128 × 128 图标：`icons/icon128.png`
- [ ] 截图 1：英文网页双语对照，1280 × 800
- [ ] 截图 2：日文网页沉浸阅读，1280 × 800
- [ ] 截图 3：侧边分栏与原文定位，1280 × 800
- [ ] 截图 4：牛津本地词典查询，1280 × 800
- [ ] 截图 5：图片 OCR 原位翻译，1280 × 800
- [ ] 440 × 280 小型宣传图
- [ ] 1400 × 560 大型宣传图

旧宣传图草稿不作为发布素材。截图必须来自真实扩展界面，不展示 API Key、个人账号、浏览历史或其他私人信息。

## 建议演示网页

- 英文：Wikipedia 的 Anthropology 条目，适合展示标题、正文、目录、链接与图片混排。
- 日文：NHK NEWS WEB EASY，适合展示日文正文、注音、图片和沉浸阅读。

## 打包边界

Chrome Web Store ZIP 只包含扩展运行所需文件，排除以下内容：

- `README*.md`、`CHROMEWEBSTORE.md`、`TODO.md`、`TESTING.md`
- `tests/`
- `assets/readme/` 与 `assets/store/`
- Git 元数据、开发脚本和本地词典文件
- API Key、导出的配置备份与测试账号信息

## 提交前检查

- [ ] 在 Chrome Test Browser 重新加载最终目录。
- [ ] 完成五类真实界面截图并检查隐私信息。
- [ ] 执行 `node tests/release-audit.mjs`。
- [ ] 执行 `node tests/translation-core-audit.mjs`。
- [ ] 在本地静态服务器上执行 `tests/playwright-runtime-audit.js` 与 `tests/playwright-ocr-audit.js` 的浏览器冒烟测试。
- [ ] 对最终 ZIP 再做文件清单与密钥扫描。
- [ ] 在商店后台核对权限、数据披露与 `PRIVACY.md` 一致。
- [ ] 首次上传后保持草稿，先处理后台警告，不提交审核。
